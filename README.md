# Prisma

## Steps for Setup

```npm install prisma typescript ts-node @types/node --save-dev```

```npx tsc --init```
```Change `rootDit` to `src` ```
``` Change `outDir` to `dist` ```

```npx prisma init```